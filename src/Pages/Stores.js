import React from 'react' 

const Stores = () => {
    return (
        <div className="store" >
            <h2>hopefully we can get like a list of stores to be routed to..we'll figure this out later</h2>
        </div>
    )
}

export default Stores